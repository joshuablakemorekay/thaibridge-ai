[pytest]
# Only collect from tests/. Without this, pytest scans the repo root and picks
# up any file named test_*.py — including one-off scripts that make live API
# calls at import time.
testpaths = tests
