# v1 — Option A: HTML templates

> **Status:** verbatim from the saved chat.

```
Yes. Create Option A now. Create other Option B, C, D later
```

**What it produced:** 6 Jinja2 templates — `base.html` (master template with the Thai-inspired design), `index.html` (homepage), `learn.html` (learning hub), `exercise.html` (quiz), `culture.html`, and `premium.html` — using a deep-red / gold / cream colour scheme with lotus symbolism.

**Why staged:** I asked for one option at a time so I could review and test each layer before moving on, rather than getting everything at once.
