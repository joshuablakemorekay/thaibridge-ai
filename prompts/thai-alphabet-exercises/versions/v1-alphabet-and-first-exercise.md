# v1 — Alphabet module + first ordering exercise

> **Status:** verbatim from the saved chat.

```
Can we add the Thai Alphabet & make it accessible to Level 1 users. Make
alphabet completion requirement before moving onto anything else.
```

Followed by a request to add a practice exercise:

```
add the Thai alphabet in alphabetical order exercise to Thai Alphabet page
```

**What it produced:** an `alphabet.html` page (44 consonants by class, vowels, tone marks), backend changes making it a Level-1 prerequisite, and a drag-and-drop "alphabetical order" exercise.

**What was wrong:** the exercise only used the first 10 consonants and wasn't the true Thai order. The module also first came as a paste-in snippet and threw `name 'app' is not defined` when run on its own.
